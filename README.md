This project is a simple and responsive **Sign-Up Form** built using **Angular Reactive Forms**.
It demonstrates how to implement:

- FormGroup & FormControl
- Built-in validators (required, email, pattern)
- Custom validator for password matching
- Dynamic error messages using Angular `@if` syntax
- Disable submit button until form is valid
